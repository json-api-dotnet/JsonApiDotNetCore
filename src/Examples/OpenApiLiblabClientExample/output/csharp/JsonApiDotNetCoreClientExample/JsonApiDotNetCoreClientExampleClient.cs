// This file was generated by liblab | https://liblab.com/

using JsonApiDotNetCoreClientExample.Config;
using JsonApiDotNetCoreClientExample.Http.Handlers;
using JsonApiDotNetCoreClientExample.Services;
using Environment = JsonApiDotNetCoreClientExample.Http.Environment;

namespace JsonApiDotNetCoreClientExample;

public class JsonApiDotNetCoreClientExampleClient : IDisposable
{
    private readonly HttpClient _httpClient;
    private readonly TokenHandler _accessTokenHandler;

    public PeopleService People { get; private set; }
    public TagsService Tags { get; private set; }
    public TodoItemsService TodoItems { get; private set; }

    public JsonApiDotNetCoreClientExampleClient(JsonApiDotNetCoreClientExampleConfig? config = null)
    {
        // HACK: Enable logging of HTTP requests and responses.
        //var retryHandler = new RetryHandler();
        var retryHandler = new RetryHandler(new ColoredConsoleLogHttpMessageHandler { InnerHandler = new SocketsHttpHandler() });

        _accessTokenHandler = new TokenHandler(retryHandler)
        {
            Header = "Authorization",
            Prefix = "Bearer",
            Token = config?.AccessToken
        };

        _httpClient = new HttpClient(_accessTokenHandler)
        {
            BaseAddress = config?.Environment?.Uri ?? Environment.Default.Uri,
            DefaultRequestHeaders = { { "user-agent", "dotnet/7.0" } }
        };

        People = new PeopleService(_httpClient);
        Tags = new TagsService(_httpClient);
        TodoItems = new TodoItemsService(_httpClient);
    }

    /// <summary>
    /// Set the environment for the entire SDK.
    /// </summary>
    public void SetEnvironment(Environment environment)
    {
        SetBaseUrl(environment.Uri);
    }

    /// <summary>
    /// Sets the base URL for entire SDK.
    /// </summary>
    public void SetBaseUrl(string baseUrl)
    {
        SetBaseUrl(new Uri(baseUrl));
    }

    /// <summary>
    /// Sets the base URL for the entire SDK.
    /// </summary>
    public void SetBaseUrl(Uri uri)
    {
        _httpClient.BaseAddress = uri;
    }

    /// <summary>
    /// Sets the access token for the entire SDK.
    /// </summary>
    public void SetAccessToken(string token)
    {
        _accessTokenHandler.Token = token;
    }

    public void Dispose()
    {
        _httpClient.Dispose();
    }
}

// c029837e0e474b76bc487506e8799df5e3335891efe4fb02bda7a1441840310c
